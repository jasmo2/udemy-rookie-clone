<!DOCTYPE html>
<html>
<head>
  <title>Udemi</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<nav class="ui menu inverted navbar page grid" style="margin-bottom: 0px">
		<%= link_to "Udem'I'", root_path, class: 'brand item'%>
        <div class="right menu">
            <% if user_signed_in? %>
                <div class="item">
                    <div class="ui white button">Signed in as <%= current_user.username %>.</div>
                </div>
                <div class="item">
                    <span class='pointing finger'>Not you?    </span>
                    <%= link_to "Sign out", destroy_user_session_path, :method => :delete,class: 'ui red button' %>
                </div>
                
                
              <% else %>
                <div class="item">
                    <%= link_to "Login in",new_user_session_path, class: 'ui green button' %> 
                </div>
                <div class="item">
                    <%= link_to "sign up", new_user_registration_path, class: 'ui blue button'%>    
                </div>
                
                
              <% end %>
                
        </div>
    </nav>
<%= yield %>
	
</body>
</html>
